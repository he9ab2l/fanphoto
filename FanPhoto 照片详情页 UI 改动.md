# FanPhoto 照片详情页 UI 改动需求说明

**文档目的：** 明确照片详情页在桌面端、平板端和手机端的 UI 重构方向、交互规则、信息布局、图片适配方式和验收标准。本文只描述产品与设计需求，不包含代码实现。

## 1. 改动总目标

当前详情页存在明显的空间利用问题：顶部标题区域占用高度，底部同时放置照片导航、图片尺寸、下载和分享，导致不同类型的内容混在同一层级；全景图和竖图在固定图片容器中显示时产生大量无效留白；右侧信息栏的滚动方式和操作按钮位置不够清晰。

本次改动应将详情页重构为**沉浸式照片查看器**。照片是主要内容，信息栏是可控制的辅助面板。桌面端采用“左侧图片、右侧信息栏”，手机端采用“全屏图片、底部可折叠信息面板”。

改动后的核心原则如下：

1. **图片优先。** 图片应获得尽可能大的有效显示区域，不被无意义的固定留白限制。

2. **信息归位。** 标题、日期、图片尺寸、文件信息和版权信息统一放入信息区域，不再分散到顶部或底部。

3. **操作分组。** 上一张和下一张属于照片导航；分享和下载属于功能操作；图片尺寸属于文件信息。三者不得混在同一条万能工具栏中。

4. **面板可折叠。** 桌面端右侧信息栏可以展开或折叠。折叠后图片应占满可用画布，不得残留空白侧栏。

5. **滚动分区。** 信息栏的标题区和操作区保持固定，中间元数据内容独立滚动。不能让所有内容一起滚动。

6. **响应式适配。** 超长图、普通横图、方图和竖图均需完整显示。手机端不得将桌面端的左右分栏直接压缩使用。

## 2. 现有问题及改动原因

| 现有问题 | 具体表现 | 改动方向 |
| --- | --- | --- |
| 顶部标题占用空间 | 标题独立放在弹窗顶部，图片区域被压缩 | 标题移动到右侧信息栏；手机端放入底部信息面板 |
| 左侧图片上下留白过多 | 超长图被放进过高的固定容器，图片上下出现大块空白 | 图片容器根据图片比例和可用区域自适应 |
| 底部按钮混杂 | 上一张、下一张、图片尺寸、下载、分享处于同一排 | 按照导航、信息、功能三类重新分组 |
| 右侧信息滚动不清晰 | 信息、标题和操作可能一起滚动，滚动后难以找到关键功能 | 固定标题区、滚动内容区、固定操作区 |
| 信息栏无法充分利用 | 展开信息时占据空间，折叠后仍需确保图片能扩展 | 折叠后右栏完全收起，图片重新获得全部空间 |
| 图标按钮识别度不足 | 仅凭图标判断分享、下载、信息栏开关 | 增加清晰的按钮语义、悬停提示和可见反馈 |
| 小屏幕布局不适配 | 左右两栏会使右侧信息变窄，文字和按钮难以使用 | 手机端改为底部 Bottom Sheet，即底部可展开面板 |

## 3. 桌面端总体布局

桌面端保持“左图右信息”的核心结构，但取消顶部独立标题区和横跨弹窗的底部万能工具栏。

### 3.1 页面分区

详情弹窗由以下区域组成：

1. **图片展示区。** 位于左侧，占据剩余空间，负责显示完整照片和照片导航。

2. **信息栏。** 位于右侧，负责显示标题、日期、元数据、标签、来源和版权信息。

3. **信息栏底部操作区。** 位于右侧信息栏底部，负责分享、下载和其他功能操作。

4. **关闭入口。** 固定在弹窗右上角，始终可见。

桌面端推荐结构如下：

```
┌─────────────────────────────────────────────┐
│                                             │
│              图片展示区       信息栏         │
│        [上一张]       [下一张]  标题           │
│                              日期             │
│                              器材与曝光        │
│                              文件信息          │
│                              标签              │
│                              来源与许可        │
│                              ─────────────    │
│                              [分享] [下载]     │
└────────────────────────────────────────── × ┘
```

### 3.2 信息栏宽度

右侧信息栏应保持稳定宽度，建议在 **280–340px** 范围内。默认宽度建议约 **320px**。信息栏不应无限变宽，否则会压缩主图区域。

信息栏可以支持用户调整宽度，但调整范围应有限制，建议最小约 280px、最大约 400px。调整宽度不能导致标题、元数据或按钮溢出。

### 3.3 标题位置

标题、日期和照片基本识别信息放在右侧信息栏的顶部。顶部不再单独设置一个横跨弹窗的标题区。

标题要求如下：

- 标题位于信息栏第一组内容；

- 日期位于标题下方；

- 标题最多显示两至三行；

- 超长标题需要有明确的截断策略，并支持查看完整标题；

- 标题区域在信息内容滚动时保持固定；

- 关闭按钮不应被长标题挤压。

## 4. 图片展示要求

### 4.1 通用原则

图片必须保持原始宽高比，不得为了填满容器而进行非等比拉伸。详情页默认优先完整展示照片，不得使用会裁剪主体的强制填充方式。

图片展示区域应使用可用空间，而不是固定设置一个过高的画布。图片的宽度和高度应同时受到以下条件约束：

- 不超过图片展示区宽度；

- 不超过弹窗内容区高度；

- 不遮挡导航按钮和关闭按钮；

- 不被底部操作区覆盖；

- 不因图片比例变化而撑开整个弹窗。

### 4.2 超长横图

对于 3:1、4:1 或更宽的全景图，应以横向宽度为主要约束，使图片尽可能铺满左侧展示区。图片高度按照原始比例自然缩小，不应被强行拉高。

超长图上下可以保留少量必要留白，但不得出现当前页面中的大面积上下空白。信息栏高度不应因超长图变矮或变高而发生异常变化。

当信息栏折叠后，超长图应使用新增的全部横向空间重新适配，但仍必须保持原始比例。

### 4.3 竖图

对于竖图，应以高度为主要约束。图片最大高度不得超过弹窗可用高度。竖图允许左右出现适量留白，但不能为了消除左右留白而裁掉图片内容。

竖图在信息栏展开和折叠时都需要重新适配展示区域。重新适配不应导致图片重新加载，也不应让图片突然跳到不可见区域。

### 4.4 正方形和普通横图

正方形图和普通横图应在宽度和高度之间取得平衡。图片应尽量使用左侧有效空间，但不能强制占满一个与图片比例不匹配的固定区域。

### 4.5 图片导航

上一张和下一张属于照片导航，应始终跟随图片展示区，而不是放在弹窗底部。

要求如下：

- 上一张位于图片展示区左侧或左侧边缘附近；

- 下一张位于图片展示区右侧或右侧边缘附近；

- 两个按钮垂直居中对齐图片；

- 点击区域不小于 44×44px；

- 按钮在复杂图片背景上仍需保持足够对比度；

- 鼠标悬停、键盘聚焦和按下状态必须有明确反馈；

- 支持键盘左右方向键切换照片；

- 按钮不能贴住图片主体，也不能被信息栏遮挡。

## 5. 信息栏结构与滚动方式

右侧信息栏不得让所有内容一起滚动。信息栏必须拆分成固定区域和滚动区域。

### 5.1 固定区域

以下内容必须固定，不随中间内容滚动：

- 标题；

- 日期；

- 信息栏展开/折叠按钮；

- 信息栏底部的分享、下载和其他操作按钮。

### 5.2 独立滚动区域

只有以下内容进入中间滚动区：

- 器材与曝光；

- 文件格式、尺寸和文件大小；

- 软件、作者和版权；

- 标签；

- 来源与许可；

- 其他可扩展元数据。

滚动区域应具有明确的滚动边界，不应带动图片展示区和整个弹窗一起滚动。信息栏内容不足时不强制显示滚动条；内容超出可用高度时才启用滚动。

### 5.3 信息分组

信息应按用户理解顺序组织：

1. 照片标题和日期；

2. 器材与曝光；

3. 文件信息；

4. 作者和版权；

5. 标签；

6. 来源与许可。

“图片尺寸”必须放在“文件信息”分组内，不再单独放在页面底部中央。分享和下载不得混入文件属性列表。

### 5.4 右侧栏底部操作区

分享和下载应放在右侧信息栏的底部，并保持固定。下载是主要操作，视觉权重可以高于分享。按钮应统一尺寸、间距和状态反馈。

建议操作顺序为：

```
[分享] [下载图片]
```

如果存在更多操作，应放入“更多”菜单，不要继续增加底部横向按钮数量。

## 6. 信息栏折叠交互

### 6.1 展开状态

默认可以显示信息栏，具体以产品希望的首次进入状态为准。展开时，左侧图片使用剩余空间，右侧显示完整信息。

展开状态下：

- 标题显示在右栏顶部；

- 中间元数据独立滚动；

- 分享和下载固定在右栏底部；

- 图片导航位于图片两侧；

- 关闭按钮位于弹窗右上角。

### 6.2 折叠状态

折叠后，右侧信息栏必须完全收起，不能留下空白列或不可用的占位区域。图片展示区应扩展到整个弹窗可用宽度。

折叠后必须保留以下按钮：

- 显示信息栏；

- 关闭详情页；

- 上一张；

- 下一张。

分享和下载可以在图片底部以浮动工具组形式显示，但默认不应过度遮挡图片。可采用悬停显示、点击显示或短暂显示的方式。

### 6.3 折叠状态按钮

折叠后的图片上只保留必要操作。右上角保留“显示信息”和“关闭”，图片左右保留照片切换，分享和下载放在底部浮动操作区。

按钮必须有清晰的图标语义、悬停提示、键盘聚焦状态和足够的点击区域。不能只使用难以理解的单一图标而没有任何文字或提示。

### 6.4 展开与折叠过渡

展开和折叠时，图片应平滑重新适配可用空间。不得出现明显闪烁、重新加载、内容跳动或焦点丢失。

用户折叠信息栏后，当前照片、当前查看状态和主要交互焦点应保持不变。重新展开后，信息栏应恢复到合理的滚动位置，不能每次都跳回无关位置。

## 7. 手机端布局

手机端不采用“左图右侧栏”。在小屏幕上，右侧栏会被压缩成窄列，造成标题、元数据和按钮难以阅读。因此手机端应改为**全屏图片加底部可折叠信息面板**。

### 7.1 默认状态

默认状态以图片展示为主：

- 图片占据屏幕主要区域；

- 顶部只保留关闭按钮和必要的界面入口；

- 上一张和下一张位于图片左右两侧；

- 底部显示“查看图片信息”入口；

- 不在顶部额外显示标题；

- 不在图片底部堆叠多个功能按钮。

### 7.2 底部信息面板

信息栏在手机端从底部弹出，而不是从右侧出现。底部面板应至少支持以下状态：

| 状态 | 显示内容 |
| --- | --- |
| 折叠 | 图片、必要导航、查看信息入口 |
| 半展开 | 标题、日期、分享和下载等主要操作 |
| 完全展开 | 完整元数据、标签、来源与许可 |

面板应有明显的拖拽指示区域和收起按钮。用户可以通过点击或上滑展开，通过下滑或点击按钮收起。

### 7.3 手机信息面板滚动

手机信息面板内部仍然采用分区滚动：

- 面板头部固定显示标题、日期和收起入口；

- 中间元数据区域独立滚动；

- 底部分享和下载操作固定；

- 面板最大高度不能覆盖全部屏幕；

- 展开信息时，背景图片不能继续响应误触操作。

### 7.4 手机图片适配

手机端图片仍需完整保持比例。

- 超长图横向铺满屏幕，高度自然缩小；

- 竖图以高度为主要限制，允许左右适量留白；

- 普通横图和方图根据屏幕可用高度合理显示；

- 不得强制裁切成统一比例；

- 图片不能被底部面板和底部安全区域遮挡。

### 7.5 手机按钮

手机端所有主要按钮的点击区域不小于 44×44px。按钮应考虑单手操作和安全区域。

常驻按钮只包括：

- 关闭；

- 上一张；

- 下一张；

- 查看信息或展开面板。

分享和下载在信息面板展开时放在面板底部；如果产品要求在折叠状态下提供，也应使用简洁的浮动按钮组，不得与导航按钮混成一排。

## 8. 平板端布局

平板端需要在“桌面左右布局”和“手机底部面板”之间设置明确的过渡策略。

在较宽的平板横屏上，可以继续使用左图右栏，但右侧栏必须保持足够宽度。在较窄的平板或平板竖屏上，应切换到底部信息面板，避免信息栏变窄。

布局切换不能只根据设备名称判断，应根据实际可用宽度和右侧栏最低可读宽度判断。任何状态下都不能让信息栏低于可读和可操作的最小宽度。

## 9. 必须同步解决的 UI 问题

本次改动不能只移动按钮，还需要同步解决以下问题：

1. 去除顶部独立标题区，避免标题重复和纵向空间浪费。

2. 去除横跨整个弹窗的底部万能工具栏。

3. 将图片尺寸移动到“文件信息”分组。

4. 将上一张和下一张移动到图片两侧。

5. 将分享和下载移动到信息栏底部固定操作区。

6. 折叠信息栏后，图片必须真正扩展到完整宽度。

7. 右侧信息栏的中间内容独立滚动，标题和操作不能滚走。

8. 超长图不得出现大面积上下空白。

9. 竖图不得为了填满宽度而裁切主体。

10. 关闭、展开信息、分享、下载、上一张和下一张都要有明确的交互反馈。

11. 图标按钮需要可理解的可访问名称和悬停提示。

12. 手机端必须处理底部安全区域，避免操作按钮贴近系统手势区。

13. 信息面板展开时，背景照片不能被误触切换或关闭。

14. 展开、折叠、切换照片时不能出现明显的布局跳动或重复加载。

## 10. 验收标准

### 10.1 桌面端

- 不再显示独立的顶部标题区。

- 标题和日期位于右侧信息栏顶部。

- 图片区域不存在因固定高度导致的大面积无效留白。

- 超长图、普通横图、方图和竖图均保持原始比例。

- 上一张和下一张位于图片两侧，而不是底部统一工具栏。

- 图片尺寸位于文件信息分组。

- 分享和下载位于右侧信息栏底部并保持固定。

- 中间元数据内容可以独立滚动。

- 折叠信息栏后没有残留空白侧栏。

- 折叠后图片能够扩展到完整可用宽度。

### 10.2 手机端

- 不再使用被压缩的左图右栏布局。

- 图片默认占据主要屏幕空间。

- 信息栏从底部展开，并支持折叠、半展开和完全展开。

- 信息面板的标题区和操作区固定，中间内容独立滚动。

- 上一张和下一张按钮有足够大的触控区域。

- 分享和下载不会与图片导航挤在同一排。

- 竖图不裁切，超长图不产生明显的纵向空白。

- 底部操作不会被系统安全区域遮挡。

### 10.3 交互与可用性

- 所有按钮都能通过键盘或触控明确操作。

- 所有图标按钮都有可理解的名称或提示。

- 折叠和展开信息栏不会造成明显闪烁或图片重新加载。

- 信息栏滚动不会带动图片区域滚动。

- 图片切换不会让信息栏显示上一张照片的元数据。

- 长标题、长版权文本和长标签不会破坏布局。

## 11. 推荐实施顺序

第一阶段先重构信息层级：移除顶部标题区和底部混合工具栏，将标题、日期和图片尺寸统一归入右侧信息栏。

第二阶段重构图片区域：取消固定高图片画布，根据图片比例和可用空间分别处理超长图、普通横图、方图和竖图。

第三阶段实现右侧信息栏的分区滚动：固定标题区，滚动中间元数据，固定底部操作区。

第四阶段加入右侧栏折叠：折叠后图片扩展至完整宽度，并在图片上保留必要的显示信息、关闭和导航按钮。

第五阶段实现手机端 Bottom Sheet：使用全屏图片、底部可折叠信息面板和固定底部操作区。

第六阶段进行多比例验收：至少覆盖 360px、390px、430px、768px、1024px、1280px 和 1440px 宽度，并分别测试超长图、竖图、普通横图和方图。

## 12. 最终产品形态

### 桌面端

> **左侧是完整照片，右侧是可折叠信息栏。标题放右侧，元数据中间滚动，分享和下载固定在右栏底部，上一张和下一张位于图片左右两侧。**

### 折叠状态

> **右侧信息栏完全消失，图片占满画布，右上角保留显示信息和关闭，图片两侧保留切换，其他操作按需浮动显示。**

### 手机端

> **不使用左图右栏，改为全屏图片加底部可展开信息面板。面板固定标题和操作，中间元数据独立滚动。**

这一套改动能够同时解决当前页面的核心问题：顶部和底部空间浪费、图片比例适配错误、按钮混杂、信息滚动不清晰、右侧栏折叠后空间利用不足，以及手机端左右布局不可用。

## References

[1]: https://test.heabl.xyz/ "FanPhoto 测试网站"